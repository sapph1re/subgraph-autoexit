{
  "name": "autoexit-polygon",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ autoexit-polygon",
    "create-local": "graph create --node http://localhost:8020/ autoexit-polygon",
    "remove-local": "graph remove --node http://localhost:8020/ autoexit-polygon",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 autoexit-polygon",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.88.0",
    "@graphprotocol/graph-ts": "0.32.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
